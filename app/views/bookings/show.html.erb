<div class="show-booking">
<h1>CONGRATULATIONS! You have booked the following event:</h1>
<div class="booking-detail">
<h2><%= @booking.event.event_name %></h2>
<p><%= @booking.ticket_amount %> tickets</p>
</div>
<h4> We can't wait to surpirse you </h4>
<h4> Remember to check you emails 24 hours before your booking, as we will tell you the exact location then :) </h4>
<ul>
  <p class="booking">
        <%= link_to "Remove",
         booking_path(@booking),
          method: :delete,
          data: { confirm: "Are you sure?" } %>
  </p>
</ul>
</div>

<%= link_to "See all my favourites", favourites_path, class: "btn btn-warning" %>