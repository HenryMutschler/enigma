<% content_for :meta_title, "#{@event.event_name} is on #{DEFAULT_META["meta_product_name"]}" %>
<% content_for :meta_description, @event.event_descritpion %>
<% content_for :meta_image, image_path(@event.image_url) %>


<div class="container">
    <br>
      <div class="event-context">
        <h4><%= @event.event_name %></h4><br>
    <div class='star-btn' id="favourite_<%= @event.id %>">
      <%= render "favourites/favourite", event: @event %>
    </div>
        <p><%= @event.event_descritpion %></p><br>
        <p>£ <%= @event.price %></p><br>
        <%= link_to 'Book Now', new_event_booking_path(@event), class:'btn btn-warning'%><br>
      </div>
      <br>
      <div id="map"
        style="width: 100%; height: 450px;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"></div>
</div>
